#include <iostream>
using namespace std;


float MySum(float a, float b)
{
    return (a + b);
}

int MySum(int a, int b, int c)
{
    return (a + b + c);
}

int MySum(int a, int b, int c, int d)
{
    return (a + b + c + d);
}

int main()
{
    int a, b, c, d;
    // when u cout Mysum u wil choose one of them every time u call the funcion.
}
